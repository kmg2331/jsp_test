<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="article">
	<insert id="insertAll" parameterType="map">
		insert into tbl_article (category, title, content, written_date, user_number) 
		values(#{category}, #{title}, #{content}, #{date}, #{userNum})
	</insert>
	<select id="selectByTitle">
		select (category, title, content, written_date, user_number) from tbl_article
		where title = #{title}
	</select>
</mapper>